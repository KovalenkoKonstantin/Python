# Еще для сортировки можно использовать функцию sorted().
# Она возвращает отсортированную копию списка (в отличии от функции sort(), которая ничего не возвращает),
# при этом исходный список не изменяется

l = [4, 1, 3, 2]
print(sorted(l))
print(l) # Функция не модифицирует исходный список
